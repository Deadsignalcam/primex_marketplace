import 'package:flutter/material.dart';

class PrimeXCallButton extends StatelessWidget {
  final String? phone;

  const PrimeXCallButton({
    super.key,
    this.phone,
  });

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
